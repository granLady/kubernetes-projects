apiVersion: apps/v1
kind: Deployment
metadata:
  name: selenium-chrome
spec:
  template:
    spec:
      containers:
        - name: selenium-chrome
          env:
            - name: GRID_TIMEOUT
              value: "60000"  # Timeout ajustado para pre-producción