apiVersion: apps/v1
kind: Deployment
metadata:
  name: selenium-hub
spec:
  template:
    spec:
      containers:
        - name: selenium-hub
          image: selenium/hub
          env:
            - name: GRID_TIMEOUT
              value: "30000" # Timeout más corto para desarrollo
