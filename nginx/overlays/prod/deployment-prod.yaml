apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment
spec:
  replicas: 5 # Update the replica count to 5
  template:
    spec:
      containers:
        - name: nginx
          image: nginx
          ports:
            - containerPort: 80
