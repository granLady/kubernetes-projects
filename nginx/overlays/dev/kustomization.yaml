apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: nginx-dev
resources:
  - ../../base
patchesStrategicMerge:
  - nginx-deployment-dev.yaml
nameSuffix: "-dev"
commonLabels:
  env: dev
  app: nginx-dev
