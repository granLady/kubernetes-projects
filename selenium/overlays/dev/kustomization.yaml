apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
helmCharts:
  - name: selenium-grid
    version: "0.40.0"
    releaseName: selenium-grid
    namespace: selenium-dev
    valuesFile: selenium-grid-values-dev.yaml
nameSuffix: "-dev"
patches:
  - target:
      kind: Ingress
      name: docker-selenium-selenium-ingress
    patch: |-
      apiVersion: networking.k8s.io/v1
      kind: Ingress
      metadata:
        annotations:
          kubernetes.io/ingress.class: "nginx"
        name: dcm-ingress-selenium-dev
        namespace: dcm-selenium-dev
      spec:
        rules:
          - host: selenium-dev.daniela.net  # Ajusta el host para el entorno de desarrollo
            http:
              paths:
                - path: /
                  pathType: Prefix
                  backend:
                    service:
                      name: docker-selenium-selenium-hub
                      port:
                        number: 4444
configurations:
  - ../../base/selenium-grid-chart-values.yaml
